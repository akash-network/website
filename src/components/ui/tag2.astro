---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {
  className?: string;
  active?: boolean;
}
const { className, active, ...props } = Astro.props;
---

<a
  class={`
${className}
${active ? `!border-primary text-primary` : ` text-sortText`}
cursor-pointer rounded-[6px] border whitespace-nowrap   bg-background2  px-[11px] py-[7px] md:px-[13px] md:py-[9px]  text-xs  font-normal leading-normal  shadow-sm hover:border-primary hover:text-primary md:text-sm`}
  {...props}
>
  <slot />
</a>
